actions:
  - entrypoint: "/bin/bash"
    commands: ["-c", "RNAfold -p -d2 --noLP --noPS < ${FASTA_FILE} > ${OUTPUT_FILE}" ]
    imageUri: "gcr.io/viroids/assemble2"
    mounts:
        - disk: gcloud-shared
          path: /data
          readOnly: false